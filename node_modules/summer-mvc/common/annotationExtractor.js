const fs = require( "fs" );
const annotation = require( "annotations" );

let controlFunction = "";

var result = annotations.getSync( require("path").join(process.cwd(), "controller", dispatchingSpec.controllerJS) );

console.log( result );
console.log( result.control.requestMapping.length );

fs.readdir( require("path").join( process.cwd(), "controller" ), function( err, items ){
  console.log( items );

  for( var i=0; i<items.length; i++ ){
    console.log( items[i] );

    let path = require( "path" );
    let temp = path.extname( require("path").join( process.cwd(), "controller", items[i] ) );

    console.log( temp );
    // console.log( path.isDirectory() );
  }
} );

function getController(){
  
}
